import { DoubleDotPipe } from './double-dot.pipe';

describe('DoubleDotPipe', () => {
  it('create an instance', () => {
    const pipe = new DoubleDotPipe();
    expect(pipe).toBeTruthy();
  });
});
